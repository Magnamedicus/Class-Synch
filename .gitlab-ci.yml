# .gitlab-ci.yml
image: node:20

cache:
  paths:
    - node_modules/

pages:
  script:
    - npm ci
    - npm run build
    - mkdir public
    - cp -r dist/* public/
  artifacts:
    paths:
      - public
  only:
    - main
